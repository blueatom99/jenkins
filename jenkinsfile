pipeline {
    agent any

    stages {
        stage('Build jenkins1') {
            steps {
                script {
                    build job: 'test1'
                }
            }
        }

        stage('Build jenkins2') {
            steps {
                script {
                    build job: 'GitHub%20jenkins/job/jenkins1/job/development/'
                }
            }
        }
    }
}
