pipeline {
    agent any

    stages {
        stage('Build jenkins1') {
            steps {
                script {
                    // Trigger job "jenkins1/master" or specific branch
                    build job: ''GitHub jenkins/job/jenkins1/job/development'
                }
            }
        }

        stage('Build jenkins2') {
            steps {
                script {
                    // Trigger job "jenkins2/master" or specific branch
                    build job: 'GitHub jenkins/job/jenkins2/job/development'
                }
            }
        }
    }
}
