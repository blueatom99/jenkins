pipeline {
    agent any

    stages {
        stage('Build jenkins1') {
            steps {
                script {
                    build job: 'GitHub jenkins/jenkins1/${env.BRANCH_NAME}'
                }
            }
        }

        stage('Build jenkins2') {
            steps {
                script {
                    build job: 'GitHub jenkins/jenkins2/development'
                }
            }
        }
    }
}
/*
Codepaths obtained with
Jenkins.instance.getAllItems().each { item ->
    println(item.fullName)
}
*/